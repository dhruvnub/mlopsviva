pipeline {
    agent any

    stages {
        
        stage('Train') {
            steps {
                sh 'python src/train.py'
            }
        }
        
    }

    post {
        success { echo 'Pipeline passed!' }
        failure { echo 'Pipeline failed.' }
    }
}