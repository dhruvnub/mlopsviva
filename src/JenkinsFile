pipeline {
    agent any

    stages {
        
        stage('Train') {
            steps {
                bat 'python src/train.py'
            }
        }
        
    }

    post {
        success { echo 'Pipeline passed!' }
        failure { echo 'Pipeline failed.' }
    }
}